import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2D6A78"
    },
    secondary: {
      main: "#66625f"
    }
  }
});
// 45, 106, 120
export default theme;
